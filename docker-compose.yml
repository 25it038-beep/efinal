version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    volumes:
      - ./backend/phishing_detector.db:/app/phishing_detector.db
    environment:
      - PORT=10000
    restart: always
